var base = base || {};
base.driverController = function () {
    var controller = {
        load: function () {}
    };
    //TODO: the controller object needs a load function
    return controller;
};